import 'package:flutter/material.dart';
import 'arcade_screen.dart';

class BiegaczScreen extends StatelessWidget {
  const BiegaczScreen({super.key});
  @override
  Widget build(BuildContext context) => const ArcadeScreen(balloon: false);
}
