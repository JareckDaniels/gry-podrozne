import 'package:flutter/material.dart';
import 'arcade_screen.dart';

class BalonScreen extends StatelessWidget {
  const BalonScreen({super.key});
  @override
  Widget build(BuildContext context) => const ArcadeScreen(balloon: true);
}
